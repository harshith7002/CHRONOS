services:
  - type: web
    name: chronos-control-plane
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn chronos.server.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
